<UserControl x:Class="VolunteerClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="400" Height="401">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🔐 Вход в систему" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,30"
                   Foreground="#2C3E50"/>

        <TextBlock Grid.Row="1" Text="Email" FontWeight="SemiBold" Margin="0,5" Foreground="#555"/>
        <TextBox Grid.Row="2" 
                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,5" 
                 Padding="12" 
                 FontSize="14"
                 BorderBrush="#BDC3C7"
                 BorderThickness="1"
                 Background="#F8F9FA"/>

        <TextBlock Grid.Row="3" Text="Пароль" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#555"/>
        <PasswordBox x:Name="PasswordBox"
                     Grid.Row="4" 
                     Margin="0,5" 
                     Padding="12" 
                     FontSize="14"
                     BorderBrush="#BDC3C7"
                     BorderThickness="1"
                     Background="#F8F9FA"/>

        <TextBlock Grid.Row="5" 
                   Text="{Binding ErrorMessage}" 
                   Foreground="#E74C3C" 
                   Margin="0,10" 
                   TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}"/>

        <Button Grid.Row="6" 
                Content="Войти" 
                Command="{Binding LoginCommand}"
                Background="#3498DB" 
                Foreground="White" 
                FontSize="16" 
                FontWeight="Bold"
                Padding="12" 
                Margin="0,20,0,10"
                BorderThickness="0"
                Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="IsEnabled" Value="True"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Content" Value="Загрузка..."/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Grid.Row="6" 
                Content="Нет аккаунта? Зарегистрироваться"
                Command="{Binding GoToRegisterCommand}"
                Background="Transparent" 
                Foreground="#3498DB"
                BorderThickness="0" 
                Margin="0,67,0,-47"
                Cursor="Hand"/>
    </Grid>
</UserControl>