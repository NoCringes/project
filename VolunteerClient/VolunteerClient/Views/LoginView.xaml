<UserControl x:Class="VolunteerClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="450" Height="374">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🔐 Вход в систему" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,30"
                   Foreground="#2C3E50"/>

        <TextBlock Grid.Row="1" Text="Email" FontWeight="SemiBold" Margin="0,5" Foreground="#555"/>
        <TextBox x:Name="EmailBox" 
                 Grid.Row="2" 
                 Margin="0,5" 
                 Padding="12" 
                 FontSize="14"
                 BorderBrush="#BDC3C7"
                 BorderThickness="1"
                 Background="#F8F9FA"/>

        <TextBlock Grid.Row="3" Text="Пароль" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="#555"/>
        <PasswordBox x:Name="PasswordBox" 
                     Grid.Row="4" 
                     Margin="0,5" 
                     Padding="12" 
                     FontSize="14"
                     BorderBrush="#BDC3C7"
                     BorderThickness="1"
                     Background="#F8F9FA"/>

        <Button x:Name="LoginButton" 
            Grid.Row="5" 
            Content="Войти" 
            Background="#3498DB" 
            Foreground="White" 
            FontSize="16" 
            FontWeight="Bold"
            Padding="12" 
            Margin="0,20,0,25"   
            BorderThickness="0"
        Cursor="Hand"/>

        <Button x:Name="GoToRegisterButton" 
            Grid.Row="5" 
            Content="Нет аккаунта? Зарегистрироваться"
            Background="Transparent" 
            Foreground="#3498DB"
            BorderThickness="0" 
            Margin="0,71,0,-13"
            Cursor="Hand"/>
    </Grid>
</UserControl>