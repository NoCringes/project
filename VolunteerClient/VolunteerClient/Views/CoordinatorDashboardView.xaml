<UserControl x:Class="VolunteerClient.Views.CoordinatorDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопками -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="10" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CreateEventButton" 
                        Content="➕ Создать мероприятие" 
                        Margin="5" 
                        Padding="10,5" 
                        Background="#27AE60" 
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
                <Button x:Name="RefreshButton" 
                        Content="🔄 Обновить" 
                        Margin="5" 
                        Padding="10,5" 
                        Background="#3498DB" 
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
                <Button x:Name="LogoutButton" 
                        Content="🚪 Выйти" 
                        Margin="5" 
                        Padding="10,5" 
                        Background="#E74C3C" 
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Список моих мероприятий -->
        <Border Grid.Row="1" Margin="10" Background="White" CornerRadius="10" 
                BorderBrush="#BDC3C7" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="📋 Мои мероприятия" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Padding="15" 
                           Background="#2C3E50" 
                           Foreground="White"/>

                <ListBox x:Name="MyEventsList" Height="500" Margin="10">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#BDC3C7" BorderThickness="0,0,0,1" Padding="10" Margin="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{Binding Location}" Foreground="Gray" FontSize="12"/>
                                        <TextBlock Text="{Binding Date}" Foreground="Gray" FontSize="12"/>
                                        <TextBlock Text="{Binding SlotsInfo}" Foreground="Gray" FontSize="12"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button x:Name="ViewVolunteersButton" 
                                                Content="👥 Волонтёры" 
                                                Margin="5" 
                                                Padding="8,5" 
                                                Background="#3498DB" 
                                                Foreground="White"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Tag="{Binding}"/>
                                        <Button x:Name="EditButton" 
                                                Content="✏️" 
                                                Margin="5" 
                                                Padding="8,5" 
                                                Background="#F39C12" 
                                                Foreground="White"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Tag="{Binding}"/>
                                        <Button x:Name="DeleteButton" 
                                                Content="🗑️" 
                                                Margin="5" 
                                                Padding="8,5" 
                                                Background="#E74C3C" 
                                                Foreground="White"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Tag="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>