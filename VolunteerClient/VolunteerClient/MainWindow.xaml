<Window x:Class="VolunteerClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VolunteerClient"
        mc:Ignorable="d"
        Title="Волонтёрская система" 
        Height="1000" 
        Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15,10">
            <Grid>
                <TextBlock Text="🤝 Волонтёрская система" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <StackPanel x:Name="UserPanel" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock x:Name="UserNameText" 
                               Foreground="White" 
                               VerticalAlignment="Center"
                               Margin="0,0,15,0"/>
                    <Button x:Name="LogoutButton" 
                            Content="Выйти" 
                            Background="#E74C3C" 
                            Foreground="White"
                            BorderThickness="0"
                            Padding="10,5"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl x:Name="MainContent" Grid.Row="1" Margin="15"/>
    </Grid>
</Window>